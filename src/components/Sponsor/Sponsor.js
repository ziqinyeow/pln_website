import classes from './Sponsor.module.css'

const Sponsor = ()=>{
    return <div className={classes['container']}>
        <div className={classes['title']}>Sponsor</div>
        
    </div>
}

export default Sponsor